<div id="<%= dom_id visit %>" class="p-8 ml-2 mt-8 border border-black sm:w-4/12">
  <p>
    <strong class="text-red-600 text-xl">Gimnasio:</strong></br>
    🦾<%= visit.gym.name %>
  </p>
  <p class="font-bold">Registraste esta visita el día</p>
  <span class="font-bold text-indigo-700"><%= visit.created_at.strftime('%d/%m/%y a las %l:%M %p') %></span>
</div>
